source "https://rubygems.org"

# Pin to the exact Jekyll/Sass versions GitHub Pages deploys, so local previews
# use the same (lib)sass and catch build differences before they ship.
gem "github-pages", group: :jekyll_plugins
