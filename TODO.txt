- [ ] Live progress counter: show [ran/total] elapsed with fail count using rich.Live.
      See full_output_improvements.patch — _build_status(), _update_status(), Live().

- [ ] Code context in failure output: show test function source around the failing line
      (like pytest's > marker). Use inspect or linecache.

- [ ] Warning filter validation (cli.py:109): assert crashes with bare AssertionError on
      bad input. e.g. warning_filters = ["error:DeprecationWarning"] (only 2 parts).
      Replace with user-friendly error message.

- [ ] Option to make -c (--changed) the default via pyproject.toml config.
      e.g. changed = true in [tool.rut]. Would need CLI flag to reverse (--all or
      --no-changed). Think through -k interaction with -c.
